use tokio::time::Instant;
use crate::automaton::client::HttpClient;


    pub async fn run_read_values(http_client:HttpClient,  cookie_value:String,last_value_ts: Instant) -> Result<bool, String> {

        // Logique spécifique
    Ok(true)
}
